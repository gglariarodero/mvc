Crearse una carpeta con el nombre del proyecto "Person" en este caso dentro de htdocs en el directorop de xampp.
Arrancar en el xampp el apache y el mysql.
Dentro de mysql crear la bbdd personas con las sentencias indicadas en el ejercicio.
Ejecutar http://localhost/person/index.php

El proyecto se ha creado en php puro, para evitar tener que instalar componentes de otros frameworks.

La ruta por defecto de apache ya llama automaticamente al index del proyecto, el cual redireccionará a la tabla  de datos de person.

Respuesta a la pregunta de priorización:

Primero abordaria el primer punto, ya que es un error de datos que puede llevar a un falseamiento de la información final (con repercusión economica). Posteriormente se revisaria la lentitud en la carga del listado y por último el nombre de la página.